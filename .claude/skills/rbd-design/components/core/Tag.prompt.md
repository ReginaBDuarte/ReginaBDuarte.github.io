**Tag** — soft pill for keywords, topics, methods. `highlight` is the bright-coral marker the deck uses to emphasize a word inside running text.

```jsx
<Tag variant="iris">Human-AI interaction</Tag>
<Tag variant="highlight">over-reliance</Tag>
<Tag variant="neutral" dot>in progress</Tag>
```
