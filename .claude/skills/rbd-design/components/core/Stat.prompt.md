**Stat** — display-figure block for a headline number. Wrap part of the value in `<em>` to coral-highlight it.

```jsx
<Stat sub="Sample" value={<>1,2<em>40</em></>} label="participants across 4 studies" />
<Stat variant="coral" size="lg" value="3×" label="faster calibrated trust" />
```
